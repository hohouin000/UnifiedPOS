namespace UnifiedPOS.Web.Infrastructure;

public static class EndpointRouteBuilderExtensions
{
    // Placeholder — custom extensions removed for .NET 6 compatibility.
    // All endpoints now use inline lambda handlers with full route paths.
}
